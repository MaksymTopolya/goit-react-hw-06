import { createSlice } from '@reduxjs/toolkit';


const slice = createSlice({
  name: 'contact',
  initialState: { value: 0 },
  reducers: {
    add(state, action) {
     
    },
    delete(state, action) {
      
    },
  },
});